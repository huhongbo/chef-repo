input {
  file {
    type => "crm-bg"
    path => ["/app/*/app/*/*/*/log/*.log"]
    charset => "GBK"
    sincedb_path => "/opt/logstash/.sincedbcrm"
  }
}
filter {
} 
output {
  redis {
    host => [ "10.78.170.11", "10.78.170.12", "10.78.170.13" ]
    data_type => "list"
    batch => "true"
    batch_events => "50"
    key => "logstash"
  }
}
